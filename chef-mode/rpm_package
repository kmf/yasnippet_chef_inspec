# name: rpm_package (Chef)
# key: rpm_package
# --
# https://docs.chef.io/resources/rpm_package
rpm_package '${1:name}' do
	allow_downgrade	${2:true}
	options				 
	package_name	 
	source				 
	timeout				 
	version				 
	action :${3:install}	
end
