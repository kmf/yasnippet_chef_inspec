# name: bff_package (Chef)
# key: bff_package
# --
# https://docs.chef.io/resources/bff_package
bff_package '${1:name}' do
	options					
	package_name		
	source					
	timeout					
	version					
	action :${2:install}
end
